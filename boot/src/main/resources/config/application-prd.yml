server:
  port: 8060
  ssl:
    key-store: classpath:sample.jks
    key-store-password: secret
    key-password: password
spring:
  datasource:
    url: jdbc:mysql://api.spicybar.cn:3306/cinema?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: cinema
    password: cinema
