server:
  port: 8060
  ssl:
    key-store: classpath:sample.jks
    key-store-password: secret
    key-password: password
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/frame?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: frame
    password: frame